// This is where all the Jest test will be written in the following sprints

/* An example of what to write
describe("Auth", () => {
  test("logs in user with valid credentials", () => {
    expect(true).toBe(true);
  });
}); */

describe("Auth", () => {
  test("placeholder", () => {
    expect(true).toBe(true);
  });
});